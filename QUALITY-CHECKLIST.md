# SWAG v55 품질 체크리스트

검증 기준일: 2026-08-30

이 문서는 "완벽"이라는 표현 대신 구현 파일, 자동 검사, 브라우저 캡처처럼 다시 확인할 수 있는 근거를 기록합니다. 소스 구현과 공개 배포는 별개이므로 공개 주소에서 확인하지 않은 상태를 배포 완료로 표시하지 않습니다.

## v55 반영 범위

| 범주 | v55 반영 근거 |
| --- | --- |
| 히어로 방향 | "파스텔 문방구"로 전환 — 하늘 · 라벤더 · 민트 · 피치 워시를 구워 넣은 크림 캔버스, 모눈 노트 도트, 흰 스티커 코드 카드(-1.2deg) + 마스킹 테이프, 딥 플럼 터미널 한 장으로 심도 유지. 구조(코드창 · 터미널 · 칩 · 로테이터 · 신뢰 체크)는 그대로 두고 표면과 모션만 갈아입힘 |
| 히어로 제목 | `--h1-fit` 자동 축소로 "필요한 건 + 가장 긴 단어"가 어떤 뷰포트에서도 줄바꿈 · 잘림 없이 한 줄 유지(리사이즈 · 폰트 로드 시 재측정). 로테이터는 딥 캔디 4색 그라디언트 + 단어 교체마다 손그림 밑줄이 다시 그어지고 별이 톡 터짐 |
| 히어로 장식 | 블롭 3 · 구름 2 · 별 5 · 꽃잎 4 · 눈 달린 동글이 2(폴짝 + 빼꼼) · 테이프 1 · 물결단 1. 전부 transform / opacity / stroke-dashoffset, 화면 밖에서는 기존 관찰자가 정지, 720px 아래에서는 꽃잎 · 구름 · 별 2개를 걷어 냄 |
| 헤더 | 밝은 히어로 위에서 흰색 반전 제거(크림 위 흰 글자는 접근성 결함). 기본 잉크 헤더가 그대로 대비 만족 |
| 인트로 커튼 | 크림 파스텔로 재도색, 흰 워드마크는 CSS fill 로 브랜드 블루 덮어쓰기, 미정의였던 `heroDrift` keyframes 정의(커튼 글로우가 실제로 처음 움직임), JS 없는 환경에서는 커튼 자체를 깔지 않음 |
| 등장 연출 | 예전에는 리빌 · 단어 분해 · 타이핑이 불투명한 커튼 뒤에서 미리 끝났음. 커튼이 걷힌 뒤 시작하도록 intro-done 게이트 추가 |
| 애니메이션 버그 수정 | 리빌 종료 상태(`transform:none`/`clip-path`)가 카드 hover 리프트 · 히어로 포인터 시차 · 칩을 죽이거나 잘라 먹던 문제를 `:not(.is-visible)` 구조로 해소. 로테이터 sheen 이 단어 교체마다 끊기던 것, FAQ 아이콘 180deg 헛돌기(225deg 로 수정), 스택 타일 border 전환 끊김, bfcache 복귀 시 확장 오버레이 잔류, 스크롤 속도 부스트 눌어붙음, 필름 자동 스크롤이 화면 밖에서 매 프레임 레이아웃을 재던 것, 코드 하이라이트 인터벌이 탭 복귀 시 화면 밖에서도 되살아나던 것, 마그네틱 인라인 transform 이 :active 눌림을 덮던 것(translate 속성으로 이전) 수정 |
| 모션 감소 | chip-ring · pulse-dot · term-live · 타이핑 등장 · 로고 스피드라인 · 모바일 메뉴 · 새 장식 전부에 정지 규칙과 정지 자세(별은 은은히, 빼꼼이는 내다본 채, 스피너는 완성 링, 꽃잎은 제거) 추가. 죽은 셀렉터(.hero-window · .hero-phone · .hero-chip-3 · .loading-mark) 정리 |
| 전역 디테일 | 버튼 스티커 눌림, 숫자 타일 모서리 별, 진행 단계 번호 젤리, FAQ 아이콘 젤리, 신뢰 체크 씰룩, 캔디 신호등 폴짝, 내비 CTA 별 — 전부 hover · 등장 순간에만 생겨 대기 프레임 비용 0 |
| 좁은 화면 | 스크롤 안내가 터미널과 겹치던 1080px 이하에서 제거, 터미널 주소는 잘리는 대신 말줄임, 코드는 오른쪽 끝 안개 마스크, 스티커 기울기 완화(≤1080px 에서 -0.5deg / 0.6deg) |

## 자동 검증 결과

| 검사 | 결과 |
| --- | --- |
| `npm run check` (콘텐츠 · 디자인 정적 검증) | 통과 |
| `npm run build` (v55, 8개 경로 생성) | 통과 |
| `node scripts/audit-links.mjs` (8개 페이지 참조 감사) | 통과 — 깨진 로컬 참조 0 |
| `npm run audit:visual` (6개 뷰포트 × 8개 경로 + 상호작용 + reduced-motion) | **1622/1622 assertions 통과** |

시각 감사가 확인하는 항목: HTTP 상태, 런타임 오류 0, 페이지 식별, 단일 h1, 가로 오버플로 없음, SUIT 로드, 잘린 · 화면 밖 텍스트 없음, 14px 미만 텍스트 없음, 깨진 이미지 없음, 320 · 390에서 44px 미만 터치 타겟 없음, 인터랙티브 겹침 없음, 로고 컨테이너 초과 없음, 자동 모션 존재, 캔버스 0개, 리빌 완료, 서술형 제목 금지, 금지 카피 없음, 내비 모드 · 라벨, 홈 · 소개의 프로젝트 미노출, 3개 행 개수 · 복제 · 속도 · 방향 · 화면 내 위치, 문의 빌더 단일 · 복수 선택 · 요약 · 클립보드, 모바일 메뉴 열림 · 라벨 · 타겟 · Escape · 포커스 복귀, reduced-motion 정지 및 행 위치.

수동 캡처로 확인한 항목: 1440 · 1100 · 768 · 390 히어로, 로테이터가 "운영 시스템"일 때 한 줄 유지, 커튼 위 블루 워드마크, 커튼이 걷힌 뒤에야 타이핑 시작, 스크롤 후 진행 · 스튜디오 · CTA 구역 정상.

## 다시 확인할 항목

- 공개 주소(Cloudflare Pages) 배포 후 OG 카드 · 파비콘 · 폰트 캐시 확인
- 실기기(iOS Safari, Android Chrome)에서 파스텔 히어로 색 재현과 틸트 체감 확인
- 관리자 도구(WAG-admin)에서 콘텐츠 게시 → Pages 재배포 흐름 확인
