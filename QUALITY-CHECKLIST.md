# SWAG v50 품질 체크리스트 — SLIPSTREAM

검증 기준일: 2026-08-28

이 문서는 “완벽”이라는 표현 대신 구현 파일, 자동 검사, 브라우저 캡처처럼 다시 확인할 수 있는 근거를 기록합니다. 소스 구현과 공개 배포는 별개이므로 GitHub Pages에서 확인하지 않은 상태를 배포 완료로 표시하지 않습니다.

## 이번 개편이 답한 문제

메인 페이지가 “AI가 만든 티가 나고 촌스럽다”는 지적이 세 번째였습니다. 원인을 여섯 개의 독립된 관점(타이포 · 색과 빛 · 레이아웃 · 모션 · 내용의 진실성 · 그림과 아이콘)으로 감사해 **71개 항목**을 뽑고, 서로 다른 네 개의 아트 디렉션을 경쟁시킨 뒤 세 명의 심사(“여전히 AI로 보이는가 / 간지가 나는가 / 만들 수 있는가”)로 골랐습니다.

가장 크게 지적된 것과 그에 대한 처리:

| 지적 | 처리 |
| --- | --- |
| 여섯 구역이 전부 `영문 눈썹 → 굵은 한글 제목 → 회색 설명 → 둥근 카드`로 같았다 | `.section-head` 컴포넌트와 눈썹 라벨 전부 삭제. 구역 제목은 **17px**이 되고 내용이 가장 큰 글자가 된다 |
| 제작 분야 카드의 그림이 회색 자리표시자 막대였다 | 카드와 목업 SVG 전부 삭제. 그 자리에 `site.json`의 **실제 항목 배열**이 들어간다 |
| 히어로가 가짜 코드 창 · 가짜 터미널 · 떠 있는 ✓ 배지 · 맥 창 신호등이었다 | 전부 삭제. 확인 가능한 한 문장을 **120px 포스터 크기**로 세운 것이 화면의 전부 |
| 터미널이 `1.2s` · `8/8` · 소유하지도 않은 `swag.studio`를 출력했다 | 삭제. 대신 `mono()` 화이트리스트를 만들어 **지어낸 값이 다시 들어오면 빌드가 실패**한다 |
| 숫자 타일 `5개` `6단계` `2인` `0건`이 서로 모순됐다 | 삭제. 소개 구역의 `PEOPLE / PUBLISHED / SUBCONTRACT / OWNERSHIP` 네 줄만 남음 |
| 여덟 종류 모서리 반경의 둥근 사각형이 112개 있었다 | 컨테이너 반경 **0**. 알약은 두 곳뿐 |
| 굵기 여섯 종 · 크기 14~17px 뒤섞임 · 제목 넷이 서로 12% 안쪽 | 굵기 **3종**, 크기 **8단계**, 제목 위계 120 → 76 → 58 → 44 → 27 |

## v50 반영 범위

| 범주 | 근거 |
| --- | --- |
| 축 | 로고 칼날에서 실측한 **16.7도**. 히어로 문장의 등장, 결, 워드마크, 밝은 띠의 위아래 절단면, 푸터 블레이드가 모두 같은 각도 |
| 바닥 | 색상환 225도를 벗어난 중성 검정 `#08080a`. 밝은 띠는 진행 방식 하나뿐(`#f0eee9`) |
| 색 규칙 | 블루 = 정체성과 구조(5개 역할). 그린 = **움직이는 것에만**. 초록 체크표시 · 상태점 없음 |
| 그림자 | 페이지 전체에서 3개(헤더 · 모바일 메뉴 · 카카오 버튼), 빛 방향 105도 하나 |
| 기술 스택 | 원래 참고하신 dev.rederx.com 형태의 아이콘 격자와 3개 행 유지. 타일 배경과 아이콘 자체 배경이 겹쳐 모서리가 두 겹으로 보이던 문제를 고침 |
| 연락 수단 | 전화번호 없음. 담당자 이름과 카카오 오픈채팅 링크만 |
| 기능 유지 | 8개 경로, 내비 5개, 문의 빌더(단일/복수 선택 · 요약 · 복사 · `?type=` 사전 선택), FAQ, SEO 메타 · 구조화 데이터, 스킵 링크, 포커스 스타일, 모바일 메뉴 전부 유지 |
| 홈 문의 | 홈에서 제작 종류를 고르면 `contact/?type=`로 그대로 넘어간다. 상태 기계는 사이트에 **하나뿐** |

## 측정한 성능

| 항목 | v49 | v50 |
| --- | ---: | ---: |
| 히어로 구역 fps (CPU 4배 느리게, 4회 중앙값) | 약 50 | **59.6** |
| 기술 스택 구역 fps (CPU 4배 느리게) | 55.3 | **60.2** |
| 스크롤 fps (스로틀 없음) | 47.6 · 47.8 | **56.9 · 57.6** |
| 화면 밖에서 도는 애니메이션 | 0개 | **0개** |
| 홈의 상시 rAF 루프 | 1개 | **1개**(기술 스택 3개 행) |

구현 중 히어로가 19fps로 떨어진 적이 있습니다. A/B로 좁힌 원인은 **마스크가 걸린 부모 안에서 자식이 움직이면 매 프레임 마스크를 다시 굽는다**는 것이었고(19.3 → 60.5, 결을 통째로 숨겼을 때), 마스크를 바닥색과 같은 불투명한 덮개로 바꿔 효과를 그대로 두고 60.1fps를 회복했습니다.

## 자동 검증 결과

| 검사 | 결과 |
| --- | --- |
| `npm run check` | 통과 |
| `npm run build` (v50, 8개 경로) | 통과 |
| `node scripts/audit-links.mjs` | 통과 — 깨진 로컬 참조 0 |
| `npm run audit:visual` | **1862/1862 assertions 통과** |

`validate.mjs`에 이번에 추가된 계약: mono 서체로 조판된 문자열이 화이트리스트 밖이면 실패, 컨테이너 모서리 반경 0, 그라디언트 · 글자 그라디언트 · `backdrop-filter` · 애니메이션 blur 금지, 그림자 4개 이하, 굵기 3종 이하, 크기 8단계 밖 금지, 눈썹 라벨과 `.section-head` 금지, 모든 홈 구역의 `data-advances` 선언.

시각 감사에 추가된 항목: 히어로 안무가 실제로 끝나는지, 첫 화면에 코드 창 · 터미널 · 배지 · 캔버스가 없는지, 200px 이상 요소의 모서리 반경, 타입 스케일 이탈, 굵기 개수.

## 다시 확인할 항목

- GitHub Pages 배포 후 실제 공개 주소에서 OG 카드 · 파비콘 · 폰트 캐시 확인
- 실제 1× 저해상도 윈도우 화면에서 굵기 280의 48px 한글 획이 너무 가늘지 않은지 확인
- 실기기(iOS Safari, Android Chrome)에서 히어로 안무 체감 확인
- 관리자 도구(WAG-admin)에서 콘텐츠 게시 → Pages 재배포 흐름 확인
