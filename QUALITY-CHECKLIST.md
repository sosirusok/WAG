# SWAG v49 품질 체크리스트

검증 기준일: 2026-08-28

이 문서는 “완벽”이라는 표현 대신 구현 파일, 자동 검사, 브라우저 캡처처럼 다시 확인할 수 있는 근거를 기록합니다. 소스 구현과 공개 배포는 별개이므로 GitHub Pages에서 확인하지 않은 상태를 배포 완료로 표시하지 않습니다.

## v49 반영 범위

| 범주 | v49 반영 근거 |
| --- | --- |
| 브랜드 | 사용자 제공 로고(블루 SWAG + 그린 스피드라인 · 스우시 · 오빗)를 벡터로 재구성해 헤더 · 푸터 · 파비콘 · OG 카드에 적용 |
| 비주얼 방향 | 쿨그레이 캔버스 + 화이트 카드 + 다크 섹션, 블루 `#2145e6` · 그린 `#5bdf9c` 브랜드 팔레트, 그라디언트 강조 (`scripts/validate.mjs`가 팔레트 존재를 검사) |
| 히어로 | 다크 코드 에디터 창 + 터미널 타이핑 연출로 교체. 배경은 blur 레이어 없이 그라디언트로 구워 합성 비용 제거 |
| 제작 분야 | 웹 · 앱 · 게임 · AI · 운영 시스템 5종. 게임은 플랫폼 제한 문구 없이 웹 · 앱 · PC · 모바일 전부 표기 |
| 연락 수단 | 전화번호 제거. 담당자 이름과 카카오 오픈채팅 링크만 노출(`data/site.json`의 `contact`에 `phone` 없음, 구조화 데이터도 `sameAs`로 대체) |
| 기술 스택 | skill-icons 46개를 다크 타일로 배치, 서로 반대 방향 3개 행(27 / -21 / 24), reduced-motion 시 정지. 일시정지 버튼은 사용자 요청으로 제거 |
| 모션 | 관성 휠 스크롤, 유리 내비, 코드 · 터미널 타이핑, 헤드라인 로테이터, 카운트업, 카드 틸트, 시인(sheen), 푸터 마퀴, 이미지 스캔, 확장 전환, clip/stagger 메뉴, 스크롤 진행 바, 타임라인 진행선 |
| 성능 | `IntersectionObserver` + `animation-play-state: paused`로 화면 밖 구역 정지, `content-visibility: auto`로 렌더 스킵. 파티클 캔버스(약 9fps)와 스크롤 스큐(약 10fps)는 A/B 측정 후 제거 |
| 웹폰트 | SUIT 2단 서브셋(core 87KB / full 508KB). 첫 화면은 core만 내려받고, core에 없는 글자가 생기면 자동으로 full 폴백 |
| 기능 유지 | 8개 경로, 데스크톱 내비 5개 라벨, 문의 빌더(단일/복수 선택 · 요약 · 복사 · `?type=` 사전 선택), FAQ, 카카오 연결, SEO 메타 · 구조화 데이터, 스킵 링크 · 포커스 스타일 전부 유지 |
| 관리자 연동 | `WAG-admin`이 `meta.version >= 49`와 전화번호 없는 `contact` 스키마를 함께 지원하도록 갱신 |

## 측정한 성능 변화

| 항목 | 이전 | 현재 |
| --- | ---: | ---: |
| 히어로 구역 fps (4× CPU 스로틀) | 14 | 약 50 |
| 기술 스택 구역 fps | — | 55.3 |
| 스크롤 fps (스로틀 없음) | 38.3 | 47.6 · 47.8 |
| 화면 밖에서 도는 애니메이션 | 28개 | 0개 |
| 첫 화면 전송량 | 645KB | 121KB |

## 자동 검증 결과

| 검사 | 결과 |
| --- | --- |
| `npm run check` (콘텐츠 · 디자인 정적 검증) | 통과 |
| `npm run build` (v49, 8개 경로 생성) | 통과 |
| `node scripts/audit-links.mjs` (8개 페이지 참조 감사) | 통과 — 깨진 로컬 참조 0 |
| `npm run audit:visual` (6개 뷰포트 × 8개 경로 + 상호작용 + reduced-motion) | **1622/1622 assertions 통과** |

시각 감사가 확인하는 항목: HTTP 상태, 런타임 오류 0, 페이지 식별, 단일 h1, 가로 오버플로 없음, SUIT 로드, 잘린 · 화면 밖 텍스트 없음, 14px 미만 텍스트 없음, 깨진 이미지 없음, 320 · 390에서 44px 미만 터치 타겟 없음, 인터랙티브 겹침 없음, 로고 컨테이너 초과 없음, 자동 모션 존재, 캔버스 0개, 리빌 완료, 서술형 제목 금지, 금지 카피 없음, 내비 모드 · 라벨, 홈 · 소개의 프로젝트 미노출, 3개 행 개수 · 복제 · 속도 · 방향 · 화면 내 위치, 문의 빌더 단일 · 복수 선택 · 요약 · 클립보드, 모바일 메뉴 열림 · 라벨 · 타겟 · Escape · 포커스 복귀, reduced-motion 정지 및 행 위치.

## 다시 확인할 항목

- GitHub Pages 배포 후 실제 공개 주소에서 OG 카드 · 파비콘 · 폰트 캐시 확인
- 실기기(iOS Safari, Android Chrome)에서 관성 스크롤과 틸트 체감 확인
- 관리자 도구(WAG-admin)에서 콘텐츠 게시 → Pages 재배포 흐름 확인
